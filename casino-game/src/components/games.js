import React, { memo } from 'react';

const Games = memo((props) => {
    const { } = props;

    return (
        <div className="games-container">
            Games
        </div>
    );
});

Games.defaultProps = {
}

export default Games;
